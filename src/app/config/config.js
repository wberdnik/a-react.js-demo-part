const config = {
    product_mode: ~window.location.host.toString().indexOf('proflead.org'),
    addCounters: () => {
       //extracted
    },
    localURL: 'http://prof.loc',//http://localhost:8888'
    phones: [
        //extracted
    ],
    icons: [
        'whatsapp',
        'telegram'
    ],
    urls: [
        //extracted
    ]
};
//
export default config;
